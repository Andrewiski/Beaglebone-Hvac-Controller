(function () {

    'use strict';

    angular.module('hvacapp').controller('hvacMonitorStateController', ['$rootScope','$state', '$stateParams', '$scope', '$log', '$q', 
        function ($rootScope, $state, $stateParams, $scope, $log, $q) {
            $log.debug("hvacMonitorController init");
           
        }
    ]);
})();